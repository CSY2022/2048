function startTimer() {
  const startTime = localStorage.getItem('startTime');
  const now = new Date().getTime();
  const twentyMinutes = 20 * 60 * 1000; // 20 分钟的毫秒数

  if (!startTime) {
    localStorage.setItemcalStorage.getItem('startTime');
  const now = new Date().getTime();
  const twentyMinutes = 20 * 60 * 1000; // 20 分钟的毫秒数
  // 如果没有开始时间，设置当前时间为开始时间
  if (!startTime) {
    localStorage.setItem
startTimer();